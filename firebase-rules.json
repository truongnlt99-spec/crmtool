{
  "_huong_dan": [
    "QUAN TRONG - doc het truoc khi dan:",
    "",
    "1. Chi dan bo rules nay SAU KHI da tao tai khoan trong Firebase Console",
    "   (Authentication > Users) va da thay UID that vao cho UID_CUA_VO ben duoi.",
    "",
    "2. Vi sao phai khoa theo UID chu khong chi 'auth != null':",
    "   Firebase Email/Password mac dinh cho phep BAT KY AI tu dang ky tai khoan.",
    "   Cau hinh Firebase nam cong khai trong index.html, nen nguoi la co the tu tao",
    "   tai khoan roi doc toan bo du lieu khach hang. Khoa theo UID thi chi dung",
    "   nhung tai khoan duoc liet ke moi vao duoc.",
    "",
    "3. MCP server khong bi anh huong: no dung service account (quyen admin,",
    "   bo qua rules hoan toan).",
    "",
    "4. Them nguoi dung moi: them mot dieu kien || auth.uid === 'UID_MOI' vao ca",
    "   .read lan .write, roi Publish lai.",
    "",
    "Chi dan phan 'rules' ben duoi vao Firebase Console > Realtime Database > Rules."
  ],

  "rules": {
    ".read": false,
    ".write": false,

    "crmData": {
      ".read": "auth != null && (auth.uid === 'UID_CUA_VO')",
      ".write": "auth != null && (auth.uid === 'UID_CUA_VO')",

      "leads": {
        "$leadId": {
          ".validate": "newData.hasChildren(['id', 'name', 'stage'])",
          "stage": {
            ".validate": "newData.val().matches(/^(leadin|baogia|follow1|follow2|follow3|nuoidaihan|won|lost)$/)"
          },
          "revenueExpected": { ".validate": "newData.isNumber() && newData.val() >= 0" },
          "revenueActual": { ".validate": "newData.isNumber() && newData.val() >= 0" }
        }
      },

      "planRevenue": { ".validate": "newData.isNumber() && newData.val() >= 0" },
      "planLeads": { ".validate": "newData.isNumber() && newData.val() >= 0" }
    }
  }
}
